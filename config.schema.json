{
  "pluginAlias": "AmbientWeatherSensors",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "apiKey": {
        "title": "API Key",
        "type": "string",
        "required": true,
        "default": "api key from ambient weather account"
      },
      "applicationKey": {
        "title": "Aplication Key",
        "type": "string",
        "required": true,
        "default": "application key from ambient weather account"
      },
      "temperatureSensors": {
        "title": "Temperature Sensors",
        "type": "boolean",
        "required": false,
        "default": false
      },
      "humiditySensors": {
        "title": "Humidity Sensors",
        "type": "boolean",
        "required": false,
        "default": false
      }
    }
  }
}